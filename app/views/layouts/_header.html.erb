<nav class="navbar navbar-default">
  <div class="container-fluid">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <a href="<%= root_path %>">
        <div class="navbar-brand">
          <img src="http://7xkbd8.com1.z0.glb.clouddn.com/pagesLogo.png" style="height:20px; padding:0px;margin:0px"> <span class="badge">Beta</span>
        </div>
      </a>
    </div>

    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
          
      <ul class="nav navbar-nav navbar-right">
        <ul class="nav navbar-nav navbar-right">
          <li><%= link_to 'EN', url_for(locale: 'en') %></li>        
          <li><%= link_to '中文', url_for(locale: 'zh') %></li>
          <li><%= link_to '한글', url_for(locale: 'ko') %></li>
           
          <% if user_signed_in? %>
           <li class="dropdown">
            <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-expanded="false">Hi <%= current_user.first_name.capitalize %> <span class="caret"></span></a>
              <ul class="dropdown-menu" role="menu">
                 <% if current_user.member.present? %>
                 <li><%= link_to t("My Profile"), member_path(current_user.member.id) %></li>
                 <% end %>
                 <li><%= link_to t("Account Setting"), edit_user_registration_path %></li>
                 <li><%= link_to t("Sign Out"), destroy_user_session_path, method: :delete %></li>
              </ul>
           </li>            
          <% else %>
            <li><%= link_to t("Sign Up"), new_user_registration_path %></li>
            <li><%= link_to t("Sign In"), new_user_session_path %></li>
          <% end %>
        </ul>
      </ul>
    </div><!-- /.navbar-collapse -->
  </div><!-- /.container-fluid -->
</nav>